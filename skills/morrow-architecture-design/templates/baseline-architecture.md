# {{project_name}} Baseline 架构（现状盘点）

> 本文件仅在 Brownfield 模式下产出（见 `project-mode.md`）。
> 做的事：基于现有代码和配置文件**表层盘点**当前系统形态，供下游 Phase 5 / 6 / 10 做变更影响分析。
> **不做的事**：不做业务逻辑分析、不生成 call graph、不解读领域细节——那是 Phase 5 组件识别之后的事。

---

## 0. 探测摘要

- **扫描时间**：{{date}}
- **扫描范围**：{{例如 "项目根目录前 3 层"}}
- **发现的构建文件**：{{例如 "package.json / pom.xml / Dockerfile"}}
- **主要语言分布**（按文件数粗估）：
  | 语言 | 文件数 | 主目录 |
  |------|--------|--------|
  |  |  |  |

## 1. 现有组件清单

> 按"能独立启动 / 独立部署 / 独立编译"的最小粒度列。不要把二级目录错当组件。

| 组件 | 语言/栈 | 入口 | 主要依赖 | 数据归属 | 备注 |
|------|---------|------|----------|----------|------|
|  |  |  |  |  |  |

## 2. 技术栈盘点

### 2.1 运行时 / 框架

| 层 | 选型 | 版本 | 来源文件 |
|----|------|------|----------|
| 语言 |  |  |  |
| Web 框架 |  |  |  |
| 持久层 |  |  |  |
| 缓存 |  |  |  |
| 消息 / 事件 |  |  |  |
| 构建工具 |  |  |  |
| 测试框架 |  |  |  |

### 2.2 核心第三方依赖（取每个构建文件 top 20）

| 依赖 | 版本 | 用途推断 | 是否可升级 |
|------|------|----------|------------|
|  |  |  |  |

### 2.3 已知被弃用 / 不再维护的依赖（需关注）

## 3. 运行时与基础设施（如能从 Docker / CI 读到）

- **容器化方式**：{{Dockerfile / docker-compose / K8s manifests / 无}}
- **基础镜像**：
- **暴露端口**：
- **CI 方式**：{{GitHub Actions / GitLab CI / Jenkins / 无}}
- **运行参数与环境变量**（列已知即可，不要猜测）：

## 4. 数据面盘点

| 数据资产 | 存储 | 归属组件 | 规模级估 | 备注 |
|----------|------|----------|----------|------|
|  |  |  |  |  |

- **迁移文件 / Schema 定义位置**：
- **是否有已知的数据契约 / 协议文档**：

## 5. 外部集成盘点

| 外部系统 | 协议 | 鉴权方式 | 在代码中的位置 |
|----------|------|----------|----------------|
|  |  |  |  |

## 6. 约束与限制（从现状推断）

- **不可变更点**（如：老客户端协议已上线，不允许破坏兼容）：
- **紧耦合点**（如：数据库 schema 被多个组件直接读写）：
- **已知技术债**（明显的 / 代码注释里提过的）：

## 7. 与 PRD 的初步匹配

> 这一节的目的是"对账"，不是做方案。Phase 5 再做完整映射。

| PRD 需求 | 现状是否已有类似能力 | 初步判断（keep / modify / add / ripple） |
|----------|----------------------|------------------------------------------|

## 8. 对下游 Phase 的输出

- **对 Phase 5 组件识别的输入**：本文件 §1 是组件清单的起点；Phase 5 在此基础上标注 `change_scope`
- **对 Phase 6 技术栈确认的输入**：本文件 §2 是默认继承栈；**Phase 6 仅在要偏离时才问用户**
- **对 Phase 10 骨架生成的输入**：本文件 §1 的组件已存在目录，**Phase 10 不重建**；仅追加 `docs/`、更新 `README.md`，变更清单落到 `adaptation-todo-v{x.y.z}.md`（按版本号绑定）

## 9. 探测置信度

- **高置信**：强信号多、版本明确、README 有现状说明
- **中置信**：部分强信号；需要在 Phase 5 再核验
- **低置信**：只有弱信号；建议用户在 Phase 0.3 结束前切换回 Greenfield，或由架构师线下补全 baseline

本次判定：`high | medium | low`
原因：

## 10. 本次探测未覆盖的盲点

> 诚实列出"没扫到 / 读不懂"的地方，防止下游以为 baseline 已经全量。

-
-

---

> **一句话判定 baseline 是否够用**：
> 如果 Phase 5 组件识别时还要反复回去读业务代码，说明本 baseline 不够——要么补 §1~§5，要么降级为 Greenfield 重做。
