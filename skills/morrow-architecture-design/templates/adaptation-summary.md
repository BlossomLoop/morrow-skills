# {{project_name}} 适配变更总览（Adaptation Summary）

> Phase 10 Brownfield 路径产出。
> 目的：把所有 `modify / new / remove` 变更聚合到**一张矩阵**，供下游开发者与 reviewer 在一处看清本次改造的全貌、回退点和风险。
> 本文件**不**替代 ADR，也**不**复述组件职责（那是 baseline 和各模块 README 的事）；只做变更对账。

---

## 1. 本次改造一句话摘要

{{例如 "在现有 monorepo 中新增 ugc-meta-agent 子服务并扩展 project-bff 路由，不动 project-admin 与 project-worker。"}}

## 2. 变更矩阵

> `change_scope` 必须与 Phase 5 组件表一致；不允许矩阵里多出 Phase 5 没登记的组件。
> `涉及 FR` 必须引用 Phase 1 的 FR-xxx；没有 FR 依据的变更禁止出现在本矩阵。

| 模块 | change_scope | 涉及 FR | 涉及 ADR | 风险（V-xxx）| 回退方案 | 预计工作量 |
|------|--------------|---------|----------|--------------|----------|------------|
|  | keep / modify / new / remove |  |  |  |  |  |

## 3. 禁止触碰清单（Do-Not-Touch）

> 明确声明本次改造中**不动**的东西，防止下游 PR 误改。

| 资产 | 类型 | 禁止理由 | 违反后果 |
|------|------|----------|----------|
|  | 代码目录 / 构建配置 / CI / 数据库 schema / 线上表 |  |  |

## 4. 执行顺序建议

> 按依赖关系列可并行的批次，帮助项目经理 / 开发者安排开工节奏。

| 批次 | 包含变更 | 前置依赖 | 可验证产物 |
|------|----------|----------|------------|
| 批次 1 |  |  |  |
| 批次 2 |  |  |  |

## 5. 回退策略

- **批次级回退**：每个批次是否可以独立回退？回退方式是？
- **跨组件回退**：若某个 `modify` 组件失败，依赖它的 `new` 组件是否要同步回退？
- **数据回退**：涉及 schema 变更的部分，是否有 down migration / 双写兼容期？

## 6. 对现有能力的影响评估

| 现有能力 | 是否受影响 | 影响程度 | 兼容性保证手段 |
|----------|------------|----------|----------------|
|  | 是 / 否 | 低 / 中 / 高 | 例如 "保留旧接口 + 新路由并行 3 个版本" |

## 7. 宪法合规性声明

- 本次所有变更是否违反任一 NON-NEGOTIABLE（P-xxx）原则？`是 / 否`
- 若"是"：
  - 违反的条款：
  - 是否已走宪法修订流程：`是 / 否 / 申请豁免`
  - 豁免到期日：

## 8. 本矩阵之外的遗留事项

> 承认"本次不做但以后要做"的事，登记到 `risk-and-evolution.md §5 W-xxx` 或 `<module>/docs/adaptation-todo-v{x.y.z}.md`（按版本号绑定）。

-

---

> **一句话判定本文件是否合格**：
> 如果一个不了解本次改造的 reviewer 看完这份文件**仍无法回答**"这次改了什么 / 哪些不动 / 出问题怎么退回"，则本文件未完成。
