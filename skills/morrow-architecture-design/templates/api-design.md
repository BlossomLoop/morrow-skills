# {{project_name}} API 设计

> 接口表必须有"服务于 FR-xxx"列，回链 `requirement-analysis.md` 的功能需求编号。
> 没有回链的接口视为"找不到需求的孤儿接口"，需要解释或删除。

## 1. API 设计原则

（本节原则建议与 `constitution.md` 的 P-xxx 保持一致。违反宪法的 API 规则不允许在本文件另行创建。）

## 2. 接口域划分

| 接口域 | 调用方 | 覆盖 FR | 说明 |
|--------|--------|---------|------|

## 3. 核心接口清单

| 接口 | 方法 | 路径 | 服务于 FR | 说明 |
|------|------|------|-----------|------|
| 示例 | POST | /v1/xxx | FR-003, FR-007 |  |

## 4. 请求与响应模型

> 每个核心模型建议含：字段、类型、是否必填、说明、示例；与 `contracts/*.md` 的 Schema 保持一致。

## 5. 错误码与错误语义

| code | HTTP | 含义 | 可重试 | 触发场景 FR |
|------|------|------|--------|-------------|

## 6. 鉴权与安全要求

（与 `constitution.md §2.3 安全` 对齐；列出具体 header / token / signature 约定。）

## 7. API 风格与兼容性决策

- 版本策略：
- 分页策略：
- 时间 / ID / 命名约定：
- 向后兼容规则：

## 8. 与 OpenAPI 契约的映射说明

参见 `api-contract-mapping.md`，确保 `api-design.md` 的接口域、请求响应模型、错误语义与 `openapi.yaml` 的 paths、components、securitySchemes 保持一致。

## 9. 未覆盖到的 FR 清单

> Phase 7 完成后，列出 `requirement-analysis.md` 中**尚未被任何接口服务**的 FR 编号，并解释原因（例如走消息队列 / 非本期 / 纯前端能力）。
> 这一列表是 Phase 12 校验"需求闭环"的依据。

| FR ID | 未覆盖原因 | 替代实现 |
|-------|------------|----------|
